# Use the environment template that matches how you run the API:
#
#   cp .env.development.example .env    # npm run start:dev (local DB/Redis)
#   cp .env.production.example .env       # production-like Node process (edit secrets)
#
# Docker stack at repo root uses root `.env.development` / `.env.production` with compose — not this file.
